import { FilterInPortPipe } from './filter-in-port.pipe';

describe('FilterInPortPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInPortPipe();
    expect(pipe).toBeTruthy();
  });
});
