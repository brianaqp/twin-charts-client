import { TonFormatPipe } from './ton-format.pipe';

describe('TonFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TonFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
